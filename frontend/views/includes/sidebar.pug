//- Sidebar Navigation
aside(class="w-64 bg-slate-900 text-slate-300 flex flex-col fixed inset-y-0 z-40")
  div(class="p-6 border-b border-slate-800")
    h1(class="text-white text-xl font-bold flex items-center gap-2")
      i(class="fas fa-brain text-blue-500")
      | KB-Manager
    div(class="mt-2 flex items-center gap-2 text-xs")
      span(class="w-2 h-2 rounded-full bg-green-500")
      span 系统正常运行
  
  nav(class="flex-1 p-4 space-y-1 overflow-y-auto")   
    div(class="text-[10px] font-bold uppercase text-slate-500 px-3 py-2 mt-2") 核心监控
    a(href="/dashboard", data-page="dashboard", class=`nav-item flex items-center gap-3 px-3 py-3 rounded-lg hover:bg-slate-800 transition ${page === 'dashboard' ? 'nav-active' : ''}`)
      i(class="fas fa-tachometer-alt w-5 text-center")
      | 状态监管仪表板

    div(class="text-[10px] font-bold uppercase text-slate-500 px-3 py-2 mt-4") 服务管理
    a(href="/services", data-page="services", class=`nav-item flex items-center gap-3 px-3 py-3 rounded-lg hover:bg-slate-800 transition ${page === 'services' ? 'nav-active' : ''}`)
      i(class="fas fa-server w-5 text-center")
      | 服务与模型管理
    a(href="/import", data-page="import", class=`nav-item flex items-center gap-3 px-3 py-3 rounded-lg hover:bg-slate-800 transition ${page === 'import' ? 'nav-active' : ''}`)
      i(class="fas fa-file-import w-5 text-center")
      | 批量文件导入

    div(class="text-[10px] font-bold uppercase text-slate-500 px-3 py-2 mt-4") 运维安全
    a(href="/backup", data-page="backup", class=`nav-item flex items-center gap-3 px-3 py-3 rounded-lg hover:bg-slate-800 transition ${page === 'backup' ? 'nav-active' : ''}`)
      i(class="fas fa-database w-5 text-center")
      | 备份与恢复
    a(href="/system", data-page="system", class=`nav-item flex items-center gap-3 px-3 py-3 rounded-lg hover:bg-slate-800 transition ${page === 'system' ? 'nav-active' : ''}`)
      i(class="fas fa-users-cog w-5 text-center")
      | 系统管理 (用户)

    div(class="text-[10px] font-bold uppercase text-slate-500 px-3 py-2 mt-2") 初始部署
    a(href="/deployment", data-page="deployment", class=`nav-item flex items-center gap-3 px-3 py-3 rounded-lg hover:bg-slate-800 transition ${page === 'deployment' ? 'nav-active' : ''}`)
      i(class="fas fa-rocket w-5 text-center")
      | 部署配置向导
      
  div(class="p-4 border-t border-slate-800")
    button(onclick="handleLogout()", class="flex items-center gap-3 px-3 py-2 text-sm text-red-400 hover:text-red-300 w-full hover:bg-slate-800 rounded-lg transition")
      i(class="fas fa-sign-out-alt")
      | 退出登录