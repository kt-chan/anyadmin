//- Log Modal
div(id="logsModal", class="modal")
  .bg-slate-900.w-3/4.h-3/4.rounded-2xl.shadow-2xl.flex.flex-col.overflow-hidden
    div(class="p-4 border-b border-slate-700 flex justify-between items-center")
      h3(class="text-white font-bold font-mono") System Logs (tail -f)
      button(onclick="hideModal('logsModal')", class="text-slate-400 hover:text-white")
        i(class="fas fa-times")
    div(class="flex-1 p-4 overflow-y-auto font-mono text-xs text-green-400 space-y-1 bg-black")
      p [INFO] 2024-05-20 14:00:01 MindIE-Service: Heartbeat received. Status: Healthy.
      p [INFO] 2024-05-20 14:00:02 Milvus-Proxy: Connection pool active: 12/50.
      p [WARN] 2024-05-20 14:00:05 Doc-Parser: Queue depth > 100. Auto-scaling triggered.
      p(class="text-slate-500") ... previous logs ...

//- Connect Service Modal
div(id="connectServiceModal", class="modal")
  .bg-white.p-8.rounded-2xl.w-full.max-w-lg
    h3(class="text-xl font-bold mb-4") 对接现有服务
    div(class="space-y-4 mb-6")
      div
        label(class="block text-xs font-bold text-slate-500 mb-1") 服务类型
        select(class="w-full border p-2 rounded-lg bg-slate-50")
          option 推理引擎 (vLLM/MindIE)
          option 向量数据库
          option 文档解析器
      div(class="grid grid-cols-3 gap-4")
        div(class="col-span-2")
          label(class="block text-xs font-bold text-slate-500 mb-1") 服务 IP 地址
          input(type="text", placeholder="192.168.x.x", class="w-full border p-2 rounded-lg")
        div
          label(class="block text-xs font-bold text-slate-500 mb-1") 端口
          input(type="text", placeholder="8000", class="w-full border p-2 rounded-lg")
      button(class="w-full border border-blue-600 text-blue-600 py-2 rounded-lg text-sm font-bold .hover_bg-blue-50")
        i(class="fas fa-plug mr-2")
        | 测试连接
    div(class="flex justify-end gap-3")
      button(onclick="hideModal('connectServiceModal')", class="px-4 py-2 text-slate-500 font-bold text-sm") 取消
      button(class="px-4 py-2 bg-blue-600 text-white rounded-lg font-bold text-sm shadow-lg shadow-blue-200") 保存配置